txt = "abcabcbb"
n=len(txt)
l=[]
subs=''
maxsub=[]

for i in range(0,n):
    if txt[i] not in l:
        l.append(txt[i])
        subs+=txt[i]
    else:
        maxsub.append(len(subs))
        subs=''+txt[i]
        l=[]

print(maxsub)
print(max(maxsub))
