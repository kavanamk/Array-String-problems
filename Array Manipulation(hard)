#brute force

"""
def arrayManipulation(n, queries):
    # Write your code here
    # Write your code here
    
    arr=[0]*n
    for q in queries:
        q[0]-=1
        q[1]-=1
        for i in range(q[0],q[1]+1):
            arr[i]+=q[2]
    return (max(arr))
    """
    
    #optimized
    
    
    
